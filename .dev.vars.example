# v3 has exactly one runtime binding. Every non-secret setting is the typed,
# Magento-generated src/generated/project-config.ts deployment artifact.
# Set this locally or with `wrangler secret put PURGE_SECRET`.
PURGE_SECRET=your-secure-purge-secret
